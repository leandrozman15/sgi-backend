generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model audit_logs {
  id        String   @id(map: "AuditLog_pkey")
  companyId String
  user      String
  module    String
  action    String
  data      Json?
  timestamp DateTime @default(now())

  @@index([companyId], map: "AuditLog_companyId_idx")
}

model carriers {
  id        String    @id(map: "Carrier_pkey")
  companyId String
  nome      String
  cnpj      String?
  ie        String?
  contacts  Json?
  endereco  Json?
  data      Json?
  createdAt DateTime  @default(now())
  updatedAt DateTime
  deletedAt DateTime?

  @@index([companyId, deletedAt], map: "Carrier_companyId_deletedAt_idx")
  @@index([companyId], map: "Carrier_companyId_idx")
}

model clients {
  id        String    @id(map: "Client_pkey")
  companyId String
  nome      String
  data      Json?
  createdAt DateTime  @default(now())
  updatedAt DateTime
  deletedAt DateTime?

  @@index([companyId, deletedAt], map: "Client_companyId_deletedAt_idx")
  @@index([companyId], map: "Client_companyId_idx")
}

model companies {
  id                           String                         @id @default(dbgenerated("(gen_random_uuid())::text"))
  name                         String
  cnpj                         String?                        @unique
  plan                         String?                        @default("basic")
  active                       Boolean?                       @default(true)
  created_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  updated_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  calibrations                 calibrations[]
  quality_complaints           quality_complaints[]
  quality_inspections          quality_inspections[]
  employees                    employees[]
  financial_transactions       financial_transactions[]
  financial_bank_transactions  financial_bank_transactions[]
  financial_salary_payments    financial_salary_payments[]
  accounting_chart_accounts    accounting_chart_accounts[]
  accounting_cost_centers      accounting_cost_centers[]
  accounting_fiscal_periods    accounting_fiscal_periods[]
  accounting_journal_entries   accounting_journal_entries[]
  accounting_journal_entry_lines accounting_journal_entry_lines[]
  accounting_accounts_receivable accounting_accounts_receivable[]
  accounting_accounts_payable  accounting_accounts_payable[]
  maintenance_downtimes        maintenance_downtimes[]
  maintenance_plans            maintenance_plans[]
  maintenance_schedules        maintenance_schedules[]
  maintenance_work_order_parts maintenance_work_order_parts[]
  maintenance_work_orders      maintenance_work_orders[]
  machine_documents            machine_documents[]
  machine_meter_readings       machine_meter_readings[]
  machines                     machines[]
  hr_attendance_records        hr_attendance_records[]
  hr_epi_deliveries            hr_epi_deliveries[]
  hr_vacations                 hr_vacations[]
  hr_work_accidents            hr_work_accidents[]
  inventory_lot_movements      inventory_lot_movements[]
  inventory_lots               inventory_lots[]
  inventory_reservations       inventory_reservations[]
  inventory_stock_balances     inventory_stock_balances[]
  inventory_storage_positions  inventory_storage_positions[]
  inventory_storage_zones      inventory_storage_zones[]
  inventory_warehouse_layouts  inventory_warehouse_layouts[]
  production_orders            production_orders[]
  products                     products[]
  purchase_orders              purchase_orders[]
  purchase_requests            purchase_requests[]
  sale_items                   sale_items[]
  sales_fiscal_documents       sales_fiscal_documents[]
  sales_fiscal_events          sales_fiscal_events[]
  fiscal_tax_apportionments    fiscal_tax_apportionments[]
  sale_status_history          sale_status_history[]
  subscription_history         subscription_history[]
  tax_service_payments         tax_service_payments[]
  user_companies               user_companies[]
  users                        users[]
  work_accidents               work_accidents[]
}

model employees {
  id                  String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  name                String
  email               String?   @unique
  document            String?   @unique
  cpf                 String?
  role                String?
  access_level        String?
  position            String?
  department          String?
  status              String?   @default("active")
  has_access          Boolean?  @default(false)
  commission          Decimal?  @db.Decimal(10, 2)
  phone               String?
  address             String?
  registration_number String?
  start_date          DateTime? @db.Timestamp(6)
  vacation_days       Int?
  banco               String?
  conta               String?
  pix                 String?
  tamanho_camisa      String?
  tamanho_calca       String?
  numero_botas        String?
  permissions         Json?
  data                Json?
  company_id          String
  created_at          DateTime? @default(now()) @db.Timestamp(6)
  updated_at          DateTime? @default(now()) @db.Timestamp(6)
  companies           companies @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model financial_transactions {
  id          String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  type        String
  amount      Decimal   @db.Decimal(10, 2)
  description String?
  date        DateTime  @db.Timestamp(6)
  company_id  String
  created_at  DateTime? @default(now()) @db.Timestamp(6)
  updated_at  DateTime? @default(now()) @db.Timestamp(6)
  companies   companies @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model financial_bank_transactions {
  id                String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id        String
  transaction_type  String
  category          String?
  description       String?
  amount            Decimal   @db.Decimal(14, 4)
  transaction_date  DateTime  @db.Timestamptz(6)
  due_date          DateTime? @db.Timestamptz(6)
  payment_method    String?
  account_name      String?
  status            String?   @default("PENDING")
  reference_type    String?
  reference_id      String?
  notes             String?
  data              Json?
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
  updated_at        DateTime  @default(now()) @db.Timestamptz(6)
  companies         companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "financial_bank_transactions_company_id_idx")
  @@index([transaction_date], map: "financial_bank_transactions_transaction_date_idx")
  @@index([status], map: "financial_bank_transactions_status_idx")
  @@index([transaction_type], map: "financial_bank_transactions_transaction_type_idx")
}

model financial_salary_payments {
  id                  String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id          String
  employee_id         String?
  employee_name       String?
  period_reference    String?
  payroll_date        DateTime? @db.Timestamptz(6)
  gross_amount        Decimal?  @db.Decimal(14, 4)
  discounts_amount    Decimal?  @db.Decimal(14, 4)
  net_amount          Decimal?  @db.Decimal(14, 4)
  payment_date        DateTime? @db.Timestamptz(6)
  payment_method      String?
  status              String?   @default("PENDING")
  notes               String?
  data                Json?
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime  @default(now()) @db.Timestamptz(6)
  companies           companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "financial_salary_payments_company_id_idx")
  @@index([employee_id], map: "financial_salary_payments_employee_id_idx")
  @@index([period_reference], map: "financial_salary_payments_period_reference_idx")
  @@index([status], map: "financial_salary_payments_status_idx")
}

model inventory_movements {
  id        String   @id(map: "InventoryMovement_pkey")
  companyId String
  data      Json?
  createdAt DateTime @default(now())
  updatedAt DateTime

  @@index([companyId], map: "InventoryMovement_companyId_idx")
}

model machines {
  id                      String                    @id @default(dbgenerated("(gen_random_uuid())::text"))
  name                    String
  model                   String?
  code                    String?                   @unique
  type                    String?
  manufacturer            String?
  serial_number           String?
  tag                     String?
  sector                  String?
  location                String?
  acquisition_date        DateTime?                 @db.Timestamp(6)
  installation_date       DateTime?                 @db.Timestamp(6)
  warranty_until          DateTime?                 @db.Timestamp(6)
  power_kw                Decimal?                  @db.Decimal(10, 3)
  voltage_v               Decimal?                  @db.Decimal(10, 3)
  pressure_bar            Decimal?                  @db.Decimal(10, 3)
  rotation_rpm            Decimal?                  @db.Decimal(10, 3)
  temperature_range       String?
  lubrication_reqs        String?
  other_inputs            String?
  required_epp            String?
  main_risks              String?
  lubrication_points      String?
  maintenance_intervals   String?
  critical_spares         String?
  manual_url              String?
  maintenance_notes       String?
  maintenance_history     Json?
  last_maintenance        DateTime?                 @db.Timestamp(6)
  next_maintenance        DateTime?                 @db.Timestamp(6)
  archived                Boolean?                  @default(false)
  data                    Json?
  status                  String?                   @default("active")
  company_id              String
  created_at              DateTime?                 @default(now()) @db.Timestamp(6)
  updated_at              DateTime?                 @default(now()) @db.Timestamp(6)
  maintenance_downtimes   maintenance_downtimes[]
  maintenance_plans       maintenance_plans[]
  maintenance_schedules   maintenance_schedules[]
  maintenance_work_orders maintenance_work_orders[]
  machine_documents       machine_documents[]
  machine_meter_readings  machine_meter_readings[]
  companies               companies                 @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model maintenance_plans {
  id                    String                  @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id            String
  machine_id            String
  title                 String
  description           String?
  maintenance_type      String?
  frequency_unit        String?
  frequency_value       Int?
  estimated_hours       Decimal?                @db.Decimal(10, 2)
  checklist             Json?
  required_parts        Json?
  active                Boolean?                @default(true)
  last_executed_at      DateTime?               @db.Timestamp(6)
  next_due_at           DateTime?               @db.Timestamp(6)
  data                  Json?
  created_at            DateTime                @default(now()) @db.Timestamp(6)
  updated_at            DateTime                @default(now()) @db.Timestamp(6)
  companies             companies               @relation(fields: [company_id], references: [id], onDelete: Cascade)
  machines              machines                @relation(fields: [machine_id], references: [id], onDelete: Cascade)
  maintenance_schedules maintenance_schedules[]

  @@index([company_id], map: "maintenance_plans_company_id_idx")
  @@index([machine_id], map: "maintenance_plans_machine_id_idx")
  @@index([company_id, machine_id], map: "maintenance_plans_company_machine_idx")
}

model maintenance_schedules {
  id                      String                    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id              String
  machine_id              String
  maintenance_plan_id     String?
  scheduled_for           DateTime                  @db.Timestamp(6)
  assigned_to_employee_id String?
  priority                String?
  status                  String?                   @default("scheduled")
  notes                   String?
  recurrence_snapshot     Json?
  data                    Json?
  created_at              DateTime                  @default(now()) @db.Timestamp(6)
  updated_at              DateTime                  @default(now()) @db.Timestamp(6)
  companies               companies                 @relation(fields: [company_id], references: [id], onDelete: Cascade)
  machines                machines                  @relation(fields: [machine_id], references: [id], onDelete: Cascade)
  maintenance_plans       maintenance_plans?        @relation(fields: [maintenance_plan_id], references: [id], onDelete: SetNull)
  maintenance_work_orders maintenance_work_orders[]

  @@index([company_id], map: "maintenance_schedules_company_id_idx")
  @@index([machine_id], map: "maintenance_schedules_machine_id_idx")
  @@index([scheduled_for], map: "maintenance_schedules_scheduled_for_idx")
  @@index([status], map: "maintenance_schedules_status_idx")
}

model maintenance_work_orders {
  id                           String                         @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id                   String
  machine_id                   String
  maintenance_schedule_id      String?
  order_number                 String?
  maintenance_type             String?
  priority                     String?
  status                       String?                        @default("open")
  opened_at                    DateTime?                      @db.Timestamp(6)
  started_at                   DateTime?                      @db.Timestamp(6)
  finished_at                  DateTime?                      @db.Timestamp(6)
  reported_by                  String?
  assigned_to                  String?
  approved_by                  String?
  failure_mode                 String?
  root_cause                   String?
  actions_taken                String?
  observations                 String?
  downtime_minutes             Int?
  labor_cost                   Decimal?                       @db.Decimal(12, 2)
  parts_cost                   Decimal?                       @db.Decimal(12, 2)
  external_cost                Decimal?                       @db.Decimal(12, 2)
  total_cost                   Decimal?                       @db.Decimal(12, 2)
  data                         Json?
  created_at                   DateTime                       @default(now()) @db.Timestamp(6)
  updated_at                   DateTime                       @default(now()) @db.Timestamp(6)
  companies                    companies                      @relation(fields: [company_id], references: [id], onDelete: Cascade)
  machines                     machines                       @relation(fields: [machine_id], references: [id], onDelete: Cascade)
  maintenance_schedules        maintenance_schedules?         @relation(fields: [maintenance_schedule_id], references: [id], onDelete: SetNull)
  maintenance_downtimes        maintenance_downtimes[]
  maintenance_work_order_parts maintenance_work_order_parts[]

  @@index([company_id], map: "maintenance_work_orders_company_id_idx")
  @@index([machine_id], map: "maintenance_work_orders_machine_id_idx")
  @@index([maintenance_schedule_id], map: "maintenance_work_orders_schedule_id_idx")
  @@index([status], map: "maintenance_work_orders_status_idx")
  @@index([order_number], map: "maintenance_work_orders_order_number_idx")
}

model maintenance_work_order_parts {
  id                        String                  @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id                String
  maintenance_work_order_id String
  raw_material_id           String?
  product_id                String?
  description               String?
  quantity                  Decimal?                @db.Decimal(12, 4)
  unit_cost                 Decimal?                @db.Decimal(12, 4)
  total_cost                Decimal?                @db.Decimal(12, 4)
  data                      Json?
  created_at                DateTime                @default(now()) @db.Timestamp(6)
  updated_at                DateTime                @default(now()) @db.Timestamp(6)
  companies                 companies               @relation(fields: [company_id], references: [id], onDelete: Cascade)
  maintenance_work_orders   maintenance_work_orders @relation(fields: [maintenance_work_order_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "maintenance_work_order_parts_company_id_idx")
  @@index([maintenance_work_order_id], map: "maintenance_work_order_parts_order_id_idx")
  @@index([raw_material_id], map: "maintenance_work_order_parts_raw_material_id_idx")
  @@index([product_id], map: "maintenance_work_order_parts_product_id_idx")
}

model machine_meter_readings {
  id            String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  machine_id    String
  meter_type    String
  reading_value Decimal   @db.Decimal(14, 4)
  unit          String?
  reading_at    DateTime  @db.Timestamp(6)
  notes         String?
  data          Json?
  created_at    DateTime  @default(now()) @db.Timestamp(6)
  updated_at    DateTime  @default(now()) @db.Timestamp(6)
  companies     companies @relation(fields: [company_id], references: [id], onDelete: Cascade)
  machines      machines  @relation(fields: [machine_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "machine_meter_readings_company_id_idx")
  @@index([machine_id], map: "machine_meter_readings_machine_id_idx")
  @@index([meter_type], map: "machine_meter_readings_meter_type_idx")
  @@index([reading_at], map: "machine_meter_readings_reading_at_idx")
}

model maintenance_downtimes {
  id                        String                   @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id                String
  machine_id                String
  maintenance_work_order_id String?
  category                  String?
  reason                    String?
  planned                   Boolean?                 @default(false)
  started_at                DateTime                 @db.Timestamp(6)
  ended_at                  DateTime?                @db.Timestamp(6)
  duration_minutes          Int?
  impact_level              String?
  notes                     String?
  data                      Json?
  created_at                DateTime                 @default(now()) @db.Timestamp(6)
  updated_at                DateTime                 @default(now()) @db.Timestamp(6)
  companies                 companies                @relation(fields: [company_id], references: [id], onDelete: Cascade)
  machines                  machines                 @relation(fields: [machine_id], references: [id], onDelete: Cascade)
  maintenance_work_orders   maintenance_work_orders? @relation(fields: [maintenance_work_order_id], references: [id], onDelete: SetNull)

  @@index([company_id], map: "maintenance_downtimes_company_id_idx")
  @@index([machine_id], map: "maintenance_downtimes_machine_id_idx")
  @@index([maintenance_work_order_id], map: "maintenance_downtimes_work_order_id_idx")
  @@index([started_at], map: "maintenance_downtimes_started_at_idx")
}

model machine_documents {
  id            String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  machine_id    String
  document_type String?
  title         String
  file_url      String
  file_name     String?
  version       String?
  issued_at     DateTime? @db.Timestamp(6)
  expires_at    DateTime? @db.Timestamp(6)
  uploaded_by   String?
  data          Json?
  created_at    DateTime  @default(now()) @db.Timestamp(6)
  updated_at    DateTime  @default(now()) @db.Timestamp(6)
  companies     companies @relation(fields: [company_id], references: [id], onDelete: Cascade)
  machines      machines  @relation(fields: [machine_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "machine_documents_company_id_idx")
  @@index([machine_id], map: "machine_documents_machine_id_idx")
  @@index([document_type], map: "machine_documents_document_type_idx")
}

model memberships {
  id        String   @id(map: "Membership_pkey")
  userId    String
  companyId String
  roles     String[]
  createdAt DateTime @default(now())

  @@unique([userId, companyId], map: "Membership_userId_companyId_key")
}

model production_orders {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  number     String    @unique
  product_id String
  quantity   Int
  status     String?   @default("pending")
  company_id String
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products   products  @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model products {
  id                           String              @id @default(dbgenerated("(gen_random_uuid())::text"))
  name                         String
  code                         String?             @unique
  sku                          String?
  published                    Boolean?            @default(false)
  gs1_code                     String?
  customer_barcode             String?
  type                         String?
  description                  String?
  price                        Decimal?            @db.Decimal(10, 2)
  current_stock                Int?
  weight                       Decimal?            @db.Decimal(10, 3)
  dangerous_goods_class        String?
  country_of_origin            String?
  ncm                          String?
  cest                         String?
  cfop                         String?
  csosn                        String?
  ipi_aliquota                 Decimal?            @db.Decimal(10, 4)
  ipi_situacao_tributaria      String?
  ipi_cod_enquadramento        String?
  icms_aliquota                Decimal?            @db.Decimal(10, 4)
  sujeito_icms_st              Boolean?
  mva_aliquota                 Decimal?            @db.Decimal(10, 4)
  valor_icms_st                Decimal?            @db.Decimal(12, 4)
  origem_mercadoria            String?
  unidade_tributavel           String?
  pis_situacao_tributaria      String?
  pis_aliquota                 Decimal?            @db.Decimal(10, 4)
  cofins_situacao_tributaria   String?
  cofins_aliquota              Decimal?            @db.Decimal(10, 4)
  icms_modalidade_bc           String?
  cod_classificacao_tributaria String?
  aliquota_ibs_uf              Decimal?            @db.Decimal(10, 4)
  aliquota_ibs_mun             Decimal?            @db.Decimal(10, 4)
  aliquota_cbs                 Decimal?            @db.Decimal(10, 4)
  image_urls                   Json?
  variants                     Json?
  raw_materials                Json?
  stages                       Json?
  technical_specs              Json?
  data                         Json?
  company_id                   String
  created_at                   DateTime?           @default(now()) @db.Timestamp(6)
  updated_at                   DateTime?           @default(now()) @db.Timestamp(6)
  production_orders            production_orders[]
  companies                    companies           @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model raw_materials {
  id                         String    @id(map: "RawMaterial_pkey")
  companyId                  String
  nome                       String
  code                       String?
  sku                        String?
  gs1Code                    String?
  customerBarcode            String?
  group                      String?
  lote                       String?
  minStock                   Decimal?  @db.Decimal(12, 4)
  maxStock                   Decimal?  @db.Decimal(12, 4)
  currentStock               Decimal?  @db.Decimal(12, 4)
  weight                     Decimal?  @db.Decimal(12, 4)
  dangerousGoodsClass        String?
  tipoMaterial               String?
  unidadeConsumo             String?
  fatorConversao             Decimal?  @db.Decimal(12, 4)
  validadeDias               Int?
  localizacaoEstoque         String?
  obsTecnicas                String?
  ncm                        String?
  cest                       String?
  cfop                       String?
  csosn                      String?
  unidadeTributavel          String?
  origem                     String?
  ipiAliquota                Decimal?  @db.Decimal(10, 4)
  ipiSituacaoTributaria      String?
  ipiCodEnquadramento        String?
  icmsAliquota               Decimal?  @db.Decimal(10, 4)
  sujeitoIcmsSt              Boolean?
  mvaAliquota                Decimal?  @db.Decimal(10, 4)
  valorIcmsSt                Decimal?  @db.Decimal(12, 4)
  pisSituacaoTributaria      String?
  pisAliquota                Decimal?  @db.Decimal(10, 4)
  cofinsSituacaoTributaria   String?
  cofinsAliquota             Decimal?  @db.Decimal(10, 4)
  icmsModalidadeBC           String?
  codClassificacaoTributaria String?
  aliquotaIBSUF              Decimal?  @db.Decimal(10, 4)
  aliquotaIBSMun             Decimal?  @db.Decimal(10, 4)
  aliquotaCBS                Decimal?  @db.Decimal(10, 4)
  fornecedores               Json?
  data                       Json?
  createdAt                  DateTime  @default(now())
  updatedAt                  DateTime
  deletedAt                  DateTime?

  @@index([companyId, deletedAt], map: "RawMaterial_companyId_deletedAt_idx")
  @@index([companyId], map: "RawMaterial_companyId_idx")
}

model sales {
  id        String                @id(map: "Sale_pkey")
  companyId String
  data      Json?
  createdAt DateTime              @default(now())
  updatedAt DateTime
  deletedAt DateTime?
  fiscal_documents sales_fiscal_documents[]
  items     sale_items[]
  history   sale_status_history[]

  @@index([companyId, deletedAt], map: "Sale_companyId_deletedAt_idx")
  @@index([companyId], map: "Sale_companyId_idx")
}

model sale_items {
  id              String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id      String
  sale_id         String
  line_number     Int?
  item_type       String?
  product_id      String?
  raw_material_id String?
  sku             String?
  description     String
  quantity        Decimal   @db.Decimal(14, 4)
  unit_price      Decimal   @db.Decimal(14, 4)
  discount        Decimal?  @db.Decimal(14, 4)
  tax_amount      Decimal?  @db.Decimal(14, 4)
  line_total      Decimal   @db.Decimal(14, 4)
  metadata        Json?
  created_at      DateTime  @default(now()) @db.Timestamptz(6)
  updated_at      DateTime  @default(now()) @db.Timestamptz(6)
  companies       companies @relation(fields: [company_id], references: [id], onDelete: Cascade)
  sales           sales     @relation(fields: [sale_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "sale_items_company_id_idx")
  @@index([sale_id], map: "sale_items_sale_id_idx")
  @@index([product_id], map: "sale_items_product_id_idx")
  @@index([raw_material_id], map: "sale_items_raw_material_id_idx")
}

model sale_status_history {
  id          String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id  String
  sale_id     String
  from_status String?
  to_status   String
  changed_by  String?
  changed_at  DateTime  @default(now()) @db.Timestamptz(6)
  reason      String?
  metadata    Json?
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime  @default(now()) @db.Timestamptz(6)
  companies   companies @relation(fields: [company_id], references: [id], onDelete: Cascade)
  sales       sales     @relation(fields: [sale_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "sale_status_history_company_id_idx")
  @@index([sale_id], map: "sale_status_history_sale_id_idx")
  @@index([to_status], map: "sale_status_history_to_status_idx")
  @@index([changed_at], map: "sale_status_history_changed_at_idx")
}

model sales_fiscal_documents {
  id                   String               @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id           String
  sale_id              String?
  document_type        String               @default("NFE")
  document_number      String?
  series               String?
  access_key           String?
  status               String?
  issue_date           DateTime?            @db.Timestamptz(6)
  authorization_date   DateTime?            @db.Timestamptz(6)
  cancellation_date    DateTime?            @db.Timestamptz(6)
  sefaz_status_code    String?
  sefaz_status_message String?
  protocol_number      String?
  xml_url              String?
  danfe_url            String?
  data                 Json?
  created_at           DateTime             @default(now()) @db.Timestamptz(6)
  updated_at           DateTime             @default(now()) @db.Timestamptz(6)
  companies            companies            @relation(fields: [company_id], references: [id], onDelete: Cascade)
  sales                sales?               @relation(fields: [sale_id], references: [id], onDelete: SetNull)
  events               sales_fiscal_events[]
  receivable_titles    accounting_accounts_receivable[]

  @@unique([company_id, access_key], map: "sales_fiscal_documents_company_access_key_key")
  @@index([company_id], map: "sales_fiscal_documents_company_id_idx")
  @@index([sale_id], map: "sales_fiscal_documents_sale_id_idx")
  @@index([document_type], map: "sales_fiscal_documents_document_type_idx")
  @@index([status], map: "sales_fiscal_documents_status_idx")
}

model sales_fiscal_events {
  id                 String                  @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id         String
  fiscal_document_id String
  event_type         String
  event_status       String?
  event_date         DateTime?               @db.Timestamptz(6)
  protocol_number    String?
  reason             String?
  data               Json?
  created_at         DateTime                @default(now()) @db.Timestamptz(6)
  updated_at         DateTime                @default(now()) @db.Timestamptz(6)
  companies          companies               @relation(fields: [company_id], references: [id], onDelete: Cascade)
  fiscal_documents   sales_fiscal_documents  @relation(fields: [fiscal_document_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "sales_fiscal_events_company_id_idx")
  @@index([fiscal_document_id], map: "sales_fiscal_events_document_id_idx")
  @@index([event_type], map: "sales_fiscal_events_event_type_idx")
  @@index([event_date], map: "sales_fiscal_events_event_date_idx")
}

model accounting_fiscal_periods {
  id                 String                        @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id         String
  period_code        String
  year               Int
  month              Int?
  quarter            Int?
  start_date         DateTime                      @db.Timestamptz(6)
  end_date           DateTime                      @db.Timestamptz(6)
  status             String?                       @default("OPEN")
  closed_at          DateTime?                     @db.Timestamptz(6)
  notes              String?
  data               Json?
  created_at         DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at         DateTime                      @default(now()) @db.Timestamptz(6)
  companies          companies                     @relation(fields: [company_id], references: [id], onDelete: Cascade)
  journal_entries    accounting_journal_entries[]
  receivable_titles  accounting_accounts_receivable[]
  payable_titles     accounting_accounts_payable[]
  tax_apportionments fiscal_tax_apportionments[]

  @@unique([company_id, period_code], map: "accounting_fiscal_periods_company_period_code_key")
  @@index([company_id], map: "accounting_fiscal_periods_company_id_idx")
  @@index([year, month], map: "accounting_fiscal_periods_year_month_idx")
  @@index([status], map: "accounting_fiscal_periods_status_idx")
}

model accounting_chart_accounts {
  id                   String                            @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id           String
  code                 String
  name                 String
  account_type         String
  account_group        String?
  parent_account_id    String?
  level                Int?
  is_postable          Boolean?                          @default(true)
  is_active            Boolean?                          @default(true)
  metadata             Json?
  created_at           DateTime                          @default(now()) @db.Timestamptz(6)
  updated_at           DateTime                          @default(now()) @db.Timestamptz(6)
  companies            companies                         @relation(fields: [company_id], references: [id], onDelete: Cascade)
  parent_account       accounting_chart_accounts?        @relation("accounting_chart_accounts_hierarchy", fields: [parent_account_id], references: [id], onDelete: SetNull)
  child_accounts       accounting_chart_accounts[]       @relation("accounting_chart_accounts_hierarchy")
  journal_entry_lines  accounting_journal_entry_lines[]
  receivable_titles    accounting_accounts_receivable[]
  payable_titles       accounting_accounts_payable[]

  @@unique([company_id, code], map: "accounting_chart_accounts_company_code_key")
  @@index([company_id], map: "accounting_chart_accounts_company_id_idx")
  @@index([account_type], map: "accounting_chart_accounts_account_type_idx")
  @@index([parent_account_id], map: "accounting_chart_accounts_parent_account_id_idx")
  @@index([is_active], map: "accounting_chart_accounts_is_active_idx")
}

model accounting_cost_centers {
  id                    String                           @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id            String
  code                  String
  name                  String
  parent_cost_center_id String?
  is_active             Boolean?                         @default(true)
  metadata              Json?
  created_at            DateTime                         @default(now()) @db.Timestamptz(6)
  updated_at            DateTime                         @default(now()) @db.Timestamptz(6)
  companies             companies                        @relation(fields: [company_id], references: [id], onDelete: Cascade)
  parent_cost_center    accounting_cost_centers?         @relation("accounting_cost_centers_hierarchy", fields: [parent_cost_center_id], references: [id], onDelete: SetNull)
  child_cost_centers    accounting_cost_centers[]        @relation("accounting_cost_centers_hierarchy")
  journal_entries       accounting_journal_entries[]
  journal_entry_lines   accounting_journal_entry_lines[]

  @@unique([company_id, code], map: "accounting_cost_centers_company_code_key")
  @@index([company_id], map: "accounting_cost_centers_company_id_idx")
  @@index([parent_cost_center_id], map: "accounting_cost_centers_parent_cost_center_id_idx")
  @@index([is_active], map: "accounting_cost_centers_is_active_idx")
}

model accounting_journal_entries {
  id                 String                           @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id         String
  entry_number       String
  entry_date         DateTime                         @db.Timestamptz(6)
  description        String?
  status             String?                          @default("DRAFT")
  source_module      String?
  source_id          String?
  fiscal_period_id   String?
  cost_center_id     String?
  total_debit        Decimal                          @default(0) @db.Decimal(14, 4)
  total_credit       Decimal                          @default(0) @db.Decimal(14, 4)
  posted_at          DateTime?                        @db.Timestamptz(6)
  metadata           Json?
  created_at         DateTime                         @default(now()) @db.Timestamptz(6)
  updated_at         DateTime                         @default(now()) @db.Timestamptz(6)
  companies          companies                        @relation(fields: [company_id], references: [id], onDelete: Cascade)
  fiscal_periods     accounting_fiscal_periods?       @relation(fields: [fiscal_period_id], references: [id], onDelete: SetNull)
  cost_centers       accounting_cost_centers?         @relation(fields: [cost_center_id], references: [id], onDelete: SetNull)
  lines              accounting_journal_entry_lines[]

  @@unique([company_id, entry_number], map: "accounting_journal_entries_company_entry_number_key")
  @@index([company_id], map: "accounting_journal_entries_company_id_idx")
  @@index([entry_date], map: "accounting_journal_entries_entry_date_idx")
  @@index([status], map: "accounting_journal_entries_status_idx")
  @@index([source_module, source_id], map: "accounting_journal_entries_source_idx")
  @@index([fiscal_period_id], map: "accounting_journal_entries_fiscal_period_id_idx")
}

model accounting_journal_entry_lines {
  id                 String                          @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id         String
  journal_entry_id   String
  account_id         String
  cost_center_id     String?
  line_number        Int
  description        String?
  debit_amount       Decimal                         @default(0) @db.Decimal(14, 4)
  credit_amount      Decimal                         @default(0) @db.Decimal(14, 4)
  reference_type     String?
  reference_id       String?
  metadata           Json?
  created_at         DateTime                        @default(now()) @db.Timestamptz(6)
  updated_at         DateTime                        @default(now()) @db.Timestamptz(6)
  companies          companies                       @relation(fields: [company_id], references: [id], onDelete: Cascade)
  journal_entries    accounting_journal_entries      @relation(fields: [journal_entry_id], references: [id], onDelete: Cascade)
  chart_accounts     accounting_chart_accounts       @relation(fields: [account_id], references: [id], onDelete: Restrict)
  cost_centers       accounting_cost_centers?        @relation(fields: [cost_center_id], references: [id], onDelete: SetNull)

  @@unique([journal_entry_id, line_number], map: "accounting_journal_entry_lines_entry_line_number_key")
  @@index([company_id], map: "accounting_journal_entry_lines_company_id_idx")
  @@index([journal_entry_id], map: "accounting_journal_entry_lines_journal_entry_id_idx")
  @@index([account_id], map: "accounting_journal_entry_lines_account_id_idx")
  @@index([cost_center_id], map: "accounting_journal_entry_lines_cost_center_id_idx")
}

model accounting_accounts_receivable {
  id                  String                        @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id          String
  fiscal_period_id    String?
  account_id          String?
  fiscal_document_id  String?
  document_number     String?
  client_name         String?
  issue_date          DateTime?                     @db.Timestamptz(6)
  due_date            DateTime?                     @db.Timestamptz(6)
  amount_original     Decimal                       @default(0) @db.Decimal(14, 4)
  amount_paid         Decimal                       @default(0) @db.Decimal(14, 4)
  amount_outstanding  Decimal                       @default(0) @db.Decimal(14, 4)
  status              String?                       @default("OPEN")
  source_module       String?
  source_id           String?
  paid_at             DateTime?                     @db.Timestamptz(6)
  notes               String?
  metadata            Json?
  created_at          DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at          DateTime                      @default(now()) @db.Timestamptz(6)
  companies           companies                     @relation(fields: [company_id], references: [id], onDelete: Cascade)
  fiscal_periods      accounting_fiscal_periods?    @relation(fields: [fiscal_period_id], references: [id], onDelete: SetNull)
  chart_accounts      accounting_chart_accounts?    @relation(fields: [account_id], references: [id], onDelete: SetNull)
  fiscal_documents    sales_fiscal_documents?       @relation(fields: [fiscal_document_id], references: [id], onDelete: SetNull)

  @@index([company_id], map: "accounting_accounts_receivable_company_id_idx")
  @@index([fiscal_period_id], map: "accounting_accounts_receivable_fiscal_period_id_idx")
  @@index([fiscal_document_id], map: "accounting_accounts_receivable_fiscal_document_id_idx")
  @@index([due_date], map: "accounting_accounts_receivable_due_date_idx")
  @@index([status], map: "accounting_accounts_receivable_status_idx")
}

model accounting_accounts_payable {
  id                  String                        @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id          String
  fiscal_period_id    String?
  account_id          String?
  document_number     String?
  supplier_name       String?
  issue_date          DateTime?                     @db.Timestamptz(6)
  due_date            DateTime?                     @db.Timestamptz(6)
  amount_original     Decimal                       @default(0) @db.Decimal(14, 4)
  amount_paid         Decimal                       @default(0) @db.Decimal(14, 4)
  amount_outstanding  Decimal                       @default(0) @db.Decimal(14, 4)
  status              String?                       @default("OPEN")
  source_module       String?
  source_id           String?
  paid_at             DateTime?                     @db.Timestamptz(6)
  notes               String?
  metadata            Json?
  created_at          DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at          DateTime                      @default(now()) @db.Timestamptz(6)
  companies           companies                     @relation(fields: [company_id], references: [id], onDelete: Cascade)
  fiscal_periods      accounting_fiscal_periods?    @relation(fields: [fiscal_period_id], references: [id], onDelete: SetNull)
  chart_accounts      accounting_chart_accounts?    @relation(fields: [account_id], references: [id], onDelete: SetNull)

  @@index([company_id], map: "accounting_accounts_payable_company_id_idx")
  @@index([fiscal_period_id], map: "accounting_accounts_payable_fiscal_period_id_idx")
  @@index([due_date], map: "accounting_accounts_payable_due_date_idx")
  @@index([status], map: "accounting_accounts_payable_status_idx")
}

model fiscal_tax_apportionments {
  id                 String                        @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id         String
  fiscal_period_id   String
  tax_type           String
  tax_regime         String?
  tax_base           Decimal?                      @db.Decimal(14, 4)
  tax_rate           Decimal?                      @db.Decimal(10, 4)
  debits_amount      Decimal?                      @db.Decimal(14, 4)
  credits_amount     Decimal?                      @db.Decimal(14, 4)
  amount_due         Decimal?                      @db.Decimal(14, 4)
  due_date           DateTime?                     @db.Timestamptz(6)
  status             String?                       @default("PENDING")
  notes              String?
  metadata           Json?
  created_at         DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at         DateTime                      @default(now()) @db.Timestamptz(6)
  companies          companies                     @relation(fields: [company_id], references: [id], onDelete: Cascade)
  fiscal_periods     accounting_fiscal_periods     @relation(fields: [fiscal_period_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "fiscal_tax_apportionments_company_id_idx")
  @@index([fiscal_period_id], map: "fiscal_tax_apportionments_fiscal_period_id_idx")
  @@index([tax_type], map: "fiscal_tax_apportionments_tax_type_idx")
  @@index([status], map: "fiscal_tax_apportionments_status_idx")
  @@index([due_date], map: "fiscal_tax_apportionments_due_date_idx")
}

model suppliers {
  id                String    @id(map: "Supplier_pkey")
  companyId         String
  nome              String
  nomeFantasia      String?
  cnpj              String?
  inscricaoEstadual String?
  tipoFornecedor    String?
  active            Boolean?  @default(true)
  observacoes       String?
  condicaoPagamento String?
  moedaPadrao       String?
  prazoEntrega      Int?
  avaliacao         Int?
  dadosBancarios    Json?
  contacts          Json?
  endereco          Json?
  data              Json?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime
  deletedAt         DateTime?

  @@index([companyId, deletedAt], map: "Supplier_companyId_deletedAt_idx")
  @@index([companyId], map: "Supplier_companyId_idx")
}

model user_companies {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  user_id    String
  company_id String
  role       String
  created_at DateTime? @default(now()) @db.Timestamp(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users      users     @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([user_id, company_id])
}

model users {
  id             String           @id
  email          String           @unique
  name           String?
  role           String?          @default("CONSULTOR")
  company_id     String?
  created_at     DateTime?        @default(now()) @db.Timestamp(6)
  updated_at     DateTime?        @default(now()) @db.Timestamp(6)
  user_companies user_companies[]
  companies      companies?       @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model warehouse_locations {
  id         String   @id(map: "WarehouseLocation_pkey")
  companyId  String
  locationId String
  data       Json?
  createdAt  DateTime @default(now())
  updatedAt  DateTime

  @@unique([companyId, locationId], map: "WarehouseLocation_companyId_locationId_key")
  @@index([companyId], map: "WarehouseLocation_companyId_idx")
}

model inventory_warehouse_layouts {
  id            String                    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  code          String
  name          String
  description   String?
  status        String?                   @default("active")
  width_meters  Decimal?                  @db.Decimal(10, 3)
  length_meters Decimal?                  @db.Decimal(10, 3)
  height_meters Decimal?                  @db.Decimal(10, 3)
  metadata      Json?
  created_at    DateTime                  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime                  @default(now()) @db.Timestamptz(6)
  companies     companies                 @relation(fields: [company_id], references: [id], onDelete: Cascade)
  storage_zones inventory_storage_zones[]

  @@unique([company_id, code], map: "inventory_warehouse_layouts_company_code_key")
  @@index([company_id], map: "inventory_warehouse_layouts_company_id_idx")
  @@index([status], map: "inventory_warehouse_layouts_status_idx")
}

model inventory_storage_zones {
  id                          String                        @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id                  String
  warehouse_layout_id         String
  code                        String
  name                        String
  zone_type                   String?
  temperature_controlled      Boolean?                      @default(false)
  humidity_controlled         Boolean?                      @default(false)
  max_weight_kg               Decimal?                      @db.Decimal(12, 3)
  max_volume_m3               Decimal?                      @db.Decimal(12, 3)
  status                      String?                       @default("active")
  metadata                    Json?
  created_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  companies                   companies                     @relation(fields: [company_id], references: [id], onDelete: Cascade)
  inventory_warehouse_layouts inventory_warehouse_layouts   @relation(fields: [warehouse_layout_id], references: [id], onDelete: Cascade)
  storage_positions           inventory_storage_positions[]

  @@unique([company_id, warehouse_layout_id, code], map: "inventory_storage_zones_company_layout_code_key")
  @@index([company_id], map: "inventory_storage_zones_company_id_idx")
  @@index([warehouse_layout_id], map: "inventory_storage_zones_layout_id_idx")
  @@index([status], map: "inventory_storage_zones_status_idx")
}

model inventory_storage_positions {
  id                      String                     @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id              String
  zone_id                 String
  code                    String
  aisle                   String?
  column_number           Int?
  level_number            Int?
  position_type           String?
  status                  String?                    @default("available")
  max_weight_kg           Decimal?                   @db.Decimal(12, 3)
  max_volume_m3           Decimal?                   @db.Decimal(12, 3)
  barcode                 String?
  qr_code                 String?
  metadata                Json?
  created_at              DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at              DateTime                   @default(now()) @db.Timestamptz(6)
  companies               companies                  @relation(fields: [company_id], references: [id], onDelete: Cascade)
  inventory_storage_zones inventory_storage_zones    @relation(fields: [zone_id], references: [id], onDelete: Cascade)
  lot_balances            inventory_stock_balances[]
  reservations            inventory_reservations[]

  @@unique([company_id, zone_id, code], map: "inventory_storage_positions_company_zone_code_key")
  @@index([company_id], map: "inventory_storage_positions_company_id_idx")
  @@index([zone_id], map: "inventory_storage_positions_zone_id_idx")
  @@index([status], map: "inventory_storage_positions_status_idx")
}

model inventory_lots {
  id               String                     @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id       String
  item_type        String
  raw_material_id  String?
  product_id       String?
  lot_code         String
  supplier_id      String?
  manufacture_date DateTime?                  @db.Timestamptz(6)
  expiration_date  DateTime?                  @db.Timestamptz(6)
  received_at      DateTime?                  @db.Timestamptz(6)
  quality_status   String?                    @default("approved")
  traceability_ref String?
  metadata         Json?
  created_at       DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime                   @default(now()) @db.Timestamptz(6)
  companies        companies                  @relation(fields: [company_id], references: [id], onDelete: Cascade)
  lot_balances     inventory_stock_balances[]
  lot_movements    inventory_lot_movements[]
  reservations     inventory_reservations[]

  @@unique([company_id, lot_code], map: "inventory_lots_company_lot_code_key")
  @@index([company_id], map: "inventory_lots_company_id_idx")
  @@index([item_type], map: "inventory_lots_item_type_idx")
  @@index([quality_status], map: "inventory_lots_quality_status_idx")
  @@index([expiration_date], map: "inventory_lots_expiration_date_idx")
}

model inventory_stock_balances {
  id                  String                      @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id          String
  lot_id              String
  storage_position_id String
  quantity_on_hand    Decimal                     @default(0) @db.Decimal(14, 4)
  quantity_reserved   Decimal                     @default(0) @db.Decimal(14, 4)
  quantity_available  Decimal                     @default(0) @db.Decimal(14, 4)
  last_counted_at     DateTime?                   @db.Timestamptz(6)
  metadata            Json?
  created_at          DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at          DateTime                    @default(now()) @db.Timestamptz(6)
  companies           companies                   @relation(fields: [company_id], references: [id], onDelete: Cascade)
  inventory_lots      inventory_lots              @relation(fields: [lot_id], references: [id], onDelete: Cascade)
  storage_positions   inventory_storage_positions @relation(fields: [storage_position_id], references: [id], onDelete: Cascade)

  @@unique([company_id, lot_id, storage_position_id], map: "inventory_stock_balances_company_lot_position_key")
  @@index([company_id], map: "inventory_stock_balances_company_id_idx")
  @@index([lot_id], map: "inventory_stock_balances_lot_id_idx")
  @@index([storage_position_id], map: "inventory_stock_balances_position_id_idx")
}

model inventory_lot_movements {
  id                      String         @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id              String
  lot_id                  String
  movement_type           String
  quantity                Decimal        @db.Decimal(14, 4)
  source_position_id      String?
  destination_position_id String?
  reference_type          String?
  reference_id            String?
  unit_cost               Decimal?       @db.Decimal(14, 4)
  total_cost              Decimal?       @db.Decimal(14, 4)
  movement_date           DateTime       @default(now()) @db.Timestamptz(6)
  performed_by            String?
  notes                   String?
  metadata                Json?
  created_at              DateTime       @default(now()) @db.Timestamptz(6)
  updated_at              DateTime       @default(now()) @db.Timestamptz(6)
  companies               companies      @relation(fields: [company_id], references: [id], onDelete: Cascade)
  inventory_lots          inventory_lots @relation(fields: [lot_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "inventory_lot_movements_company_id_idx")
  @@index([lot_id], map: "inventory_lot_movements_lot_id_idx")
  @@index([movement_type], map: "inventory_lot_movements_movement_type_idx")
  @@index([movement_date], map: "inventory_lot_movements_movement_date_idx")
  @@index([source_position_id], map: "inventory_lot_movements_source_position_id_idx")
  @@index([destination_position_id], map: "inventory_lot_movements_destination_position_id_idx")
}

model inventory_reservations {
  id                  String                      @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id          String
  lot_id              String
  storage_position_id String
  quantity            Decimal                     @db.Decimal(14, 4)
  reservation_type    String?
  reference_type      String?
  reference_id        String?
  status              String?                     @default("active")
  reserved_by         String?
  reserved_at         DateTime                    @default(now()) @db.Timestamptz(6)
  expires_at          DateTime?                   @db.Timestamptz(6)
  released_at         DateTime?                   @db.Timestamptz(6)
  notes               String?
  metadata            Json?
  created_at          DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at          DateTime                    @default(now()) @db.Timestamptz(6)
  companies           companies                   @relation(fields: [company_id], references: [id], onDelete: Cascade)
  inventory_lots      inventory_lots              @relation(fields: [lot_id], references: [id], onDelete: Cascade)
  storage_positions   inventory_storage_positions @relation(fields: [storage_position_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "inventory_reservations_company_id_idx")
  @@index([lot_id], map: "inventory_reservations_lot_id_idx")
  @@index([storage_position_id], map: "inventory_reservations_position_id_idx")
  @@index([status], map: "inventory_reservations_status_idx")
  @@index([expires_at], map: "inventory_reservations_expires_at_idx")
}

model calibrations {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id String
  data       Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model quality_inspections {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id String
  data       Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model quality_complaints {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id String
  data       Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model purchase_requests {
  id                  String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id          String
  numero_solicitacao  String?
  solicitante_nome    String?
  proveedor_id        String?
  tipo                String?
  justificativa       String?
  prioridade          String?
  data_necessidade    DateTime? @db.Timestamptz(6)
  moneda              String?
  produtos            Json?
  status              String?
  estado              String?
  ordem_compra_numero String?
  data                Json?
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime  @default(now()) @db.Timestamptz(6)
  companies           companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model purchase_orders {
  id                String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id        String
  numero_ordem      String?
  solicitud_id      String?
  proveedor_id      String?
  solicitante_nome  String?
  prazo_entrega     DateTime? @db.Timestamptz(6)
  forma_pagamento   String?
  transportadora_id String?
  observacoes       String?
  produtos          Json?
  estado            String?
  fecha_orden       DateTime? @db.Timestamptz(6)
  data              Json?
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
  updated_at        DateTime  @default(now()) @db.Timestamptz(6)
  companies         companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model subscription_history {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id String
  data       Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model tax_service_payments {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id String
  data       Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model work_accidents {
  id         String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id String
  data       Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  companies  companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id])
}

model hr_vacations {
  id            String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  employee_id   String?
  employee_name String?
  start_date    DateTime? @db.Timestamptz(6)
  end_date      DateTime? @db.Timestamptz(6)
  status        String?
  notes         String?
  data          Json?
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  companies     companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "hr_vacations_company_id_idx")
  @@index([employee_id], map: "hr_vacations_employee_id_idx")
  @@index([start_date], map: "hr_vacations_start_date_idx")
}

model hr_attendance_records {
  id            String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  employee_id   String?
  employee_name String?
  date          DateTime? @db.Timestamptz(6)
  status        String?
  reason        String?
  data          Json?
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  companies     companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "hr_attendance_records_company_id_idx")
  @@index([employee_id], map: "hr_attendance_records_employee_id_idx")
  @@index([date], map: "hr_attendance_records_date_idx")
}

model hr_epi_deliveries {
  id            String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  employee_id   String?
  employee_name String?
  delivery_date DateTime? @db.Timestamptz(6)
  items         Json?
  notes         String?
  data          Json?
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  companies     companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "hr_epi_deliveries_company_id_idx")
  @@index([employee_id], map: "hr_epi_deliveries_employee_id_idx")
  @@index([delivery_date], map: "hr_epi_deliveries_delivery_date_idx")
}

model hr_work_accidents {
  id            String    @id @default(dbgenerated("(gen_random_uuid())::text"))
  company_id    String
  employee_id   String?
  employee_name String?
  accident_date DateTime? @db.Timestamptz(6)
  location      String?
  severity      String?
  status        String?
  description   String?
  actions_taken String?
  data          Json?
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  companies     companies @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id], map: "hr_work_accidents_company_id_idx")
  @@index([employee_id], map: "hr_work_accidents_employee_id_idx")
  @@index([accident_date], map: "hr_work_accidents_accident_date_idx")
}
